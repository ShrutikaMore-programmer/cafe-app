name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Deploy to AWS EC2 via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /home/ubuntu
          
          # 1. Clean up old container and image if they exist
          docker stop cafe-container || true
          docker rm cafe-container || true
          docker rmi cafe-image || true
          
          # 2. Delete old project directory to avoid conflicts
          rm -rf cafe-app
          
          # 3. Clone a completely fresh copy of your repository code
          git clone https://github.com{{ github.repository }}.git cafe-app
          cd cafe-app
          
          # 4. Build the fresh Docker image and run it
          docker build -t cafe-image .
          docker run -d --name cafe-container -p 8000:8000 cafe-image